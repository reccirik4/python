# ============================================================
# DubSync Pro v1.0 — GPU Bağımlılıkları
# ============================================================
# RTX 5050 Blackwell (sm_120) + PyTorch nightly cu128
#
# Kurulum (temiz venv):
#   py -3.12 -m venv venv
#   venv\Scripts\activate
#   pip install -r requirements_gpu.txt
#   python patch_torchaudio_torchcodec.py
#
# NOT: torch nightly ayrı index URL gerektirir, bu yüzden
#      iki aşamalı kurulum yapılır (aşağıya bakın).
# ============================================================

# === GUI ===
PyQt6>=6.10.0

# === SRT / Altyazı İşleme ===
pysrt>=1.1.2
chardet>=5.0.0

# === Ses Üretimi (Ücretsiz TTS) ===
edge-tts>=7.0.0
aiohttp>=3.9.0

# === Ses İşleme ===
numpy>=2.4.0
soundfile>=0.12.0
librosa>=0.11.0
pydub>=0.25.1

# === Video İşleme ===
# FFmpeg sistemde yüklü olmalıdır (PATH'te)

# === Ses Klonlama — XTTS-v2 (GPU) ===
coqui-tts==0.27.5
transformers>=4.57.0
torchcodec>=0.10.0

# ============================================================
# !!! TORCH NIGHTLY AYRI KURULUR !!!
# ============================================================
# requirements.txt içinde --index-url desteklenmediği için
# torch ve torchaudio ayrı komutla kurulmalıdır:
#
#   pip install --pre torch torchaudio --index-url https://download.pytorch.org/whl/nightly/cu128
#
# Ardından torchcodec Windows fix:
#   python patch_torchaudio_torchcodec.py
#
# Doğrulama:
#   python -c "import torch; print(torch.__version__, torch.cuda.is_available())"
# ============================================================

# === OPSİYONEL ===
# pyrubberband>=0.4.0          # Kaliteli time stretch
# openai>=1.0.0                # OpenAI TTS API
# elevenlabs>=1.0.0            # ElevenLabs TTS API
